from setuptools import setup, find_packages

setup(
    name='finances-nes',
    version='1.0',
    description='A finances backend app for NES POO course',
    install_requires=[],
    packages=find_packages(),
)